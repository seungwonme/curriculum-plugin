# 저자 의도 - 이 스킬이 왜 이렇게 설계됐나 (헌법)

> 커리큘럼 제작과 개선의 공통 의도. 새 규칙을 더하거나 자료를 고치기 전 이 원칙과 맞는지 확인한다. 검수/제작 시작 전 1회 읽는다.
> 경로 표기: authoring.md = `curriculum-authoring` 스킬, review.md = `curriculum-review` 스킬, image-generation-notion-assets.md = `curriculum-notion-sync` 스킬의 references.

## 한 문장

AI 창작이 아니라 사람이 만든 검증된 기존 자료를 베이스로 **약간의 개선만** 한다. 분석에서 멈추지 말고 실제로 고치되, **AI 자율 미감으로 새로 짓거나 멀쩡한 자료를 임의 재작성하지 않는다.**

## 3대 실패 패턴 (게이트가 막으려는 것)

1. **기존 자료를 안 보고 새로 지어냄**
   - 기존 검증 자료가 기본값이다. 신규 창작은 빠진 연결부에만. 강제: `explore` 게이트(authoring 3-1절 / review 0절).
2. **검수가 약점표/분석에서 멈춤**
   - 검수 = 실제로 고쳐진 자료. 제안, 약점 나열은 검수가 아니다. 실행 계약은 review.md 4절이며, `gate-review`는 반영 전 기계 전제만 확인한다. 실제 개선은 인용과 diff로 확인한다.
3. **AI 미감으로 품질을 판단**
   - 기존 검증 자료와 명시된 성공 기준을 기준점으로 삼는다. AI 취향만으로 보존/교체를 결정하지 않는다.

## 판단 원칙

- **기본값은 보존, 개선은 최소** - "개선"은 명시적 결함(누락, 오류, 낡음, AI 티 표기)만 외과적으로 고치는 것이다. 멀쩡한 톤, 구조, 이미지를 AI 취향으로 재작성하지 않는다. "약간의 개선만"이 사용자가 원하는 폭이다.
- **이미지는 기존/공식 자료 우선** - 원본의 실제 스크린샷, 로고, 도식이 있으면 그대로 이식한다. 개념 이미지는 공식 문서나 신뢰 가능한 기존 자료에서 찾는다. 생성은 세상에 실물이 없는 이번 회차의 net-new 산출물 결과 화면이나 목업에만 쓴다. 정본: image-generation-notion-assets.md intro.
- **AI 판단을 신뢰의 기준점으로 쓰지 않는다** - 좋다/나쁘다, 맞다/안 맞다를 AI 미감으로 결정하지 말고, 사람이 만든 검증 자료를 기준점(gold standard)으로 둔다. 확신이 안 서면 혼자 단정하지 말고 사용자에게 묻는다.
- **보여주면 말하지 않는다, 빼라면 완전히 뺀다** - 이미지, 링크, 북마크가 이미 보여주는 것을 그 위/아래에 안내 텍스트나 callout으로 또 설명하지 않는다("가입은 아래에서" 류 중복 금지). "빼라"는 지시는 요약해서 남기는 게 아니라 흔적 없이 제거다 - 섹션마다 도입, 안내 문장을 깔아야 한다는 강박을 버린다. 빈 섹션이 빈 안내보다 낫다.
- **반복 자체보다 매번 새로 지어내는 변형을 막는다** - 같은 보안 경고나 안내를 반복해야 하면 검증된 문구를 그대로 재사용한다.

## 균형 (한쪽으로 치우치지 않기)

"더 하라"가 아니라 "핵심만, 끝까지"다. 빈약화(신호 없는 설명 과잉)와 비대화(잡설, 과한 절차, 기존보다 디테일하게 다시 쓰기) **양쪽 모두 반려**된다. 원본 밀도가 분량 상한선이다(authoring 3-5절). 특히 **현 버전 UI에서만 통하는 세세한 절차(가입 단계, 버튼명, 설정값)는 금방 stale되니 링크와 실제 캡처로 대체**한다(anti-patterns.md 3절).
